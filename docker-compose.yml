services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/dev.db
      - PORT=5000
      - CLIENT_URL=http://localhost:3000
      - JWT_SECRET=${JWT_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - JWT_ISSUER=ExtremeGearShop
      - JWT_AUDIENCE=client
      - JWT_EXPIRES_IN=15m
    volumes:
      - ./server/dev.db:/app/dev.db
    restart: unless-stopped

  client:
    build: ./client
    ports:
      - "3000:80"
    volumes:
      - ./client/public/images:/usr/share/nginx/html/images   # ← уже добавили
    depends_on:
      - server
    environment:
      - REACT_APP_API_URL=http://localhost:5000/api
    restart: unless-stopped
